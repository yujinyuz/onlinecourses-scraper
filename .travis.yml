sudo: false

language: python
python:
  - "2.6"
  - "2.7"

before_install:
  - python --version
  # command to install dependencies
install:
  - pip install -r requirements.txt
  - pip install codecov

# command to run tests
script:
 # - python scraper.py get-courses --pages=1
 coverage run scraper.py get-courses --pages=1

after_success:
  - codecov
