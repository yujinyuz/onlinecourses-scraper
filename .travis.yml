sudo: false

language: python
python:
  - "2.7"
  # Fix to support python 3.5 and up
  # - "3.5"
  # - "3.6"

before_install:
  - python --version
# command to install dependencies
install:
  - pip install -r requirements.txt
  - pip install codecov
  
# command to run tests
script:
 # - python scraper.py get-courses --pages=1
 coverage run scraper.py get-courses --pages=1

after_success:
  - codecov
